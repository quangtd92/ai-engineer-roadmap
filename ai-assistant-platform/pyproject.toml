[project]
name = "ai-assistant-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tdquang", email = "taquang22@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ai-assistant-platform = "ai_assistant_platform.main:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
